/**
 * Client-side entry point
 *
 * Imports CSS (triggers Vite/PostCSS compilation) and @jant/core client.
 */

// CSS must be imported here to trigger Vite's build pipeline
import "./style.css";

// Interactive components from @jant/core
import "@jant/core/client";
